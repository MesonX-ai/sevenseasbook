# Local Hero Video

Place your licensed cloud background video in this folder with the exact filename:

- `clouds.mp4`

Optional additional format for broader compatibility:

- `clouds.webm`

The homepage video layer is already configured to load from:

- `/videos/clouds.mp4`

If `clouds.mp4` is missing, the site falls back to the static hero image.
